version: '3.8'

services:
  terminal-server:
    image: your-image-name
    container_name: terminal-server
    ports:
      - "8080:80"
    environment:
      - ENV_VAR_NAME=value
    volumes:
      - ./data:/data
    restart: always